document.querySelectorAll('form[data-enquiry]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('Discovery call enquiry — DataVault Solutions');const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nOrganisation: ${data.get('company')}\nInterest: ${data.get('service')}\n\n${data.get('message')}`);window.location.href=`mailto:contact@datavaultsolutions.co.uk?subject=${subject}&body=${body}`;}));
